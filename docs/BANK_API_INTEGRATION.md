# 🏦 銀行API連携仕様書 (Bank API Integration Spec)

**関連判断**: DEC-2026-02-06-01  
**連携先**: GMOあおぞらネット銀行  
**目的**: FCF（フリーキャッシュフロー）入力の完全自動化

---

## 1. 概要
本プロジェクトでは、GMOあおぞらネット銀行のAPI（Sunabarおよび本番API）を利用し、入出金明細を自動取得するシステムを構築する。  
取得したデータは「会社OS」のMCP (Model Context Protocol) サーバーを通じて、Copilotから直接参照・集計可能にする。

---

## 2. API接続要件
- **利用API**: GMOあおぞらネット銀行API
- **契約形態**: プライベートアクセス（自社利用）
- **認証**: トークンベース認証（`x-access-token`ヘッダー）
  - OAuth 2.0ではなく、Sunabarポータルから直接トークン取得
- **SDK**: 公式Python SDKは不使用、直接HTTP通信（`requests`ライブラリ）
- **接続テスト**: ✅ 完了（2026-02-06）

---

## 3. システム構成

### MCPサーバー (`company-os-bank-mcp`)
- **実装状況**: ✅ 完了（2026-02-07）
- **提供ツール**:
  - `get_account_balance()`: 口座残高の取得（現在・前日・前月）
  - `get_transactions(days)`: 指定期間の入出金明細取得
  - `get_monthly_summary(year, month)`: 月次サマリー（FCF分析用）
  - `generate_fcf_data(year, month, app_name)`: **NEW** FCF入力データ自動生成
  - `analy（実装完了）
1. ユーザー (CEO) が Copilot に「今月のFCFデータを生成して」と質問
2. Copilot が MCP ツール `generate_fcf_data` を呼び出し
3. MCPサーバーが銀行APIから指定月の入出金明細を取得
4. App Store入金を自動識別（摘要に「Apple」「App Store」等が含まれる）
5. Apple手数料30%を自動計算
6. `data/fcf_input.csv` に追加するCSV行を生成
7. CEOがその行をコピー&ペーストして確定

### 資金移動監視フロー（NEW）
1. ユーザーが「過去30日の資金移動を分析して」と質問
2. Copilot が `analyze_cash_flow` を呼び出し
3. x] Sunabarアカウント登録（CEO）✅ 2026-02-06
- [x] Python仮想環境セットアップ ✅ 2026-02-06
- [x] 依存パッケージインストール（FastMCP, requests等）✅ 2026-02-06

### Step 2: 接続テスト
- [x] アクセストークン取得（Sunabarポータル）✅ 2026-02-06
- [x] 残高照会テスト ✅ 2026-02-06
- [x] 入出金明細照会テスト ✅ 2026-02-06

### Step 3: MCPサーバー実装
- [x] MCPサーバーのボイラープレート作成 ✅ 2026-02-06
- [x] 基本ツール定義（残高・明細・サマリー）✅ 2026-02-06
- [x] エラーハンドリング ✅ 2026-02-06
- [x] VS Code統合テスト ✅ 2026-02-07
- [x] 高度なツール追加（FCF生成・資金分析・複数口座）✅ 2026-02-07

### Step 4: 会社OS統合
- [x] FCF自動生成ツール実装 ✅ 2026-02-07
- [ ] `data/fcf_input.csv` への自動追記モード実装（手動コピペで代替可能）
- [ ] `FCF_REPORT.md` 自動更新フロー構築（次フェーズ）

### Step 2: 接続テスト
- [ ] アクセストークン取得スクリプト作成
- [ ] 残高照会テスト
- [ ] 入出金明細照会テスト

### Step 3: MCPサーバー実装
- [ ] MCPサーバーのボイラープレート作成
- [ ] ツール定義 (`get_account_balance`, `get_transactions`)
- [ ] エラーハンドリング（トークン切れ時の再認証など）

### Step 4: 会社OS統合
- [ ] `FCF_REPORT.md` 生成ロジックの実装
- [ ] `data/fcf_input.csv` への自動追記モード実装
実装完了）

### 月次FCF確定フロー
1. 月末、CEOがCopilotに「今月のFCFデータを生成して」と指示
2. Copilotが銀行APIから指定月の明細を取得
3. App Store入金を自動識別し、手数料30%を自動計算
4. 生成されたCSV行を表示
5. CEOがその行を `data/fcf_input.csv` にコピー&ペースト
6. Copilotに「FCFレポートを更新して」と指示して完了

### 資金移動監視フロー
1. 週次または任意のタイミングで「過去7日の資金移動を分析して」と指示
2. 大きな入出金があれば自動アラート
3. 異常なパターンがあればCEOが確認

### 複数口座管理（本番移行後）
1. 「全口座を表示して」で一覧確認
2. 各口座の残高を確認
3. 資金配分の最適化判断

---

## 7. 今後の拡張

### Phase 7（将来実装）
- `data/fcf_input.csv` への完全自動書き込み（CEO承認付き）
- `FCF_REPORT.md` の自動更新
- 本番API移行（Sunabar → GMO青空銀行本番環境）
- 複数口座の一括管理・統合レポート
- 週次・月次の自動アラート通知
  - アクセストークン、クライアントID等は環境変数 (`.env`) で管理
  - `.env` は `.gitignore` に含め、リポジトリにコミットしない
- **アクセス制限**:
  - 参照系APIのみ使用（振込等の更新系APIは使用しない）

---

## 6. 運用フロー（After）
1. 月末、CEOがCopilotに「今月のFCFを確定して」と指示
2. Copilotが銀行APIから明細を取得
3. Copilotが「以下の内容で `data/fcf_input.csv` を更新しますか？」と提案
4. 承認後、ファイル更新＆ `FCF_REPORT.md` 生成
